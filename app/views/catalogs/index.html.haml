%h1 Listing catalogs

%table.table.table-striped
  %thead
    %tr
      %th.col-sm-4 Name
      %th.col-sm-4 Description
      %th.col-sm-4

  %tbody
    - @catalogs.each do |catalog|
      %tr
        %td= catalog.name
        %td= truncate(catalog.description, length: 150)
        %td.text-right
          = link_to 'Create Product', new_product_path(catalog_id: catalog.id), class: "btn btn-primary"
          = link_to 'Show', catalog, class: "btn btn-default"
          = link_to 'Edit', edit_catalog_path(catalog), class: "btn btn-success"
          = link_to 'Destroy', catalog, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"

%br

= link_to 'New Catalog', new_catalog_path
